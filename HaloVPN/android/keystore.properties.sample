# Production signing credentials (replace with real values in secure storage, not checked in)
# This sample is intended to guide CI/CD or local developer setup.

storeFile=release.keystore
storePassword=yourStorePassword
keyAlias=yourKeyAlias
keyPassword=yourKeyPassword
