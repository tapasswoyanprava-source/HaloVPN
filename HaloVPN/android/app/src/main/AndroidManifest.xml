<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.haloVPN.android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:name=".MainApplication"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:supportsRtl="true">
        <!-- VPN service entry (replace with actual class if different) -->
        <service
            android:name=".HaloVpnService"
            android:exported="true"
            android:permission="android.permission.BIND_VPN_SERVICE" />

        <!-- Optional foreground service (VPN related) -->
        <service
            android:name=".HaloVpnForegroundService"
            android:exported="false" />

        <!-- Additional VPN-related components can be declared here -->

    </application>

</manifest>
